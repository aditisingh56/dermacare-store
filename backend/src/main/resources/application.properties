# application.properties
spring.application.name=dermacare-store

# Set active profile: 'h2' for in-memory database (no setup required)
# Switch to 'mysql' to use your local MySQL instance configured in application-mysql.properties
spring.profiles.active=h2